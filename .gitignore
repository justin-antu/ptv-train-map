# Dependencies
node_modules/

# Build output
dist/

# Environment variables (never commit real secrets)
.env
.env.local
.env.*.local

# Raw GTFS downloads (large; only the processed JSON is committed)
gtfs-download/
*.zip

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log
